initSidebarItems({"constant":[["MAX_QUEUE_NUM",""],["MAX_VRING_LEN",""]],"fn":[["create_vu_console_device","Return a new vhost-user console device. `params` are the device’s configuration, and `keep_rds` is a vector into which `RawDescriptors` that need to survive a fork are added, in case the device is meant to run within a child process."],["run_console_device","Starts a vhost-user console device. Returns an error if the given `args` is invalid or the device fails to run."]],"struct":[["ConsoleBackend",""],["Options","Console device"],["VhostUserConsoleDevice","Console device for use with vhost-user. Will set stdin back to canon mode if we are getting input from it."]]});